## Server (Java)
## Copy to `server/.env.local` and fill values (do not commit)

# App
JWT_SECRET=change_me_to_a_long_random_string
JWT_EXPIRATION_MS=86400000
CORS_ALLOWED_ORIGINS=http://localhost:5173

# Database (Postgres)
DB_URL=jdbc:postgresql://localhost:5432/fintrack
DB_USERNAME=fintrack
DB_PASSWORD=fintrack_password

# Plaid
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox

## Server (Java)
## Copy to `server/.env.local` and fill values (do not commit)

# App
JWT_SECRET=change_me_to_a_long_random_string
JWT_EXPIRATION_MS=86400000
CORS_ALLOWED_ORIGINS=http://localhost:5173

# Database (Postgres)
DB_URL=jdbc:postgresql://localhost:5432/fintrack
DB_USERNAME=fintrack
DB_PASSWORD=fintrack_password

# Plaid
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox

